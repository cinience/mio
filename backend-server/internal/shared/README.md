# internal/shared

Utility helpers that are safe to use across layers (retry, pooling, buffer, cleanup, queue, cryptography). Runtime teams should put technology-agnostic helpers here instead of scattering them under domain or server packages.
